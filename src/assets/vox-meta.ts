import { blockMetaInfo } from './block-meta-info'
import { testmap } from './map/test-sectors'
import { blockTextureMetaInfo } from './texture/block'

export let voxMeta = {
  blockMetaInfo: blockMetaInfo,
  voxWorldMap: testmap,
  blockTextureMetaInfo: blockTextureMetaInfo,
}